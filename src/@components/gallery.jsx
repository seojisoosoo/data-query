import React from "react";
import { getGalleryInfo } from "../api/galleryInfo";

export default function gallery(props) {
  const { no, author, meterial, name, coverThumb } = props;

  return <></>;
}
